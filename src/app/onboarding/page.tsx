"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {Check,ChevronRight,Stethoscope} from "lucide-react";import {Button} from "@/components/ui/button";
const steps=["Tu veterinaria","Identidad visual","Sucursal principal","Todo listo"];
export default function Page(){const [step,setStep]=useState(0);const router=useRouter();return <main className="min-h-screen bg-slate-50"><header className="border-b bg-white px-5 py-4"><div className="mx-auto flex max-w-5xl items-center gap-2 font-bold"><Stethoscope className="text-blue-600"/> Configuración inicial</div></header><div className="mx-auto grid max-w-5xl gap-8 px-5 py-10 md:grid-cols-[240px_1fr]"><ol className="space-y-3">{steps.map((s,i)=><li key={s} className={`flex items-center gap-3 text-sm ${i<=step?"font-semibold text-slate-900":"text-slate-400"}`}><span className={`flex h-8 w-8 items-center justify-center rounded-full ${i<step?"bg-emerald-500 text-white":i===step?"bg-blue-600 text-white":"bg-slate-200"}`}>{i<step?<Check size={17}/>:i+1}</span>{s}</li>)}</ol><section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm sm:p-9"><p className="text-sm font-semibold text-blue-600">Paso {step+1} de {steps.length}</p><h1 className="mt-2 text-2xl font-bold">{steps[step]}</h1><p className="mt-2 text-slate-500">{step===0?"Completá los datos que va a ver tu equipo.":step===1?"Elegí el estilo que representa a tu negocio.":step===2?"Indicá dónde atendés habitualmente.":"Ya podés empezar a organizar tu veterinaria."}</p>{step<3?<div className="mt-7 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">{step===0?"Nombre comercial":step===1?"Color principal":"Nombre de la sucursal"}<input type={step===1?"color":"text"} className="mt-1.5 min-h-12 w-full rounded-xl border border-slate-300 px-4" defaultValue={step===1?"#2563eb":undefined}/></label><label className="text-sm font-medium">{step===0?"Teléfono":step===1?"Logo (opcional)":"Dirección"}<input type={step===1?"file":"text"} className="mt-1.5 min-h-12 w-full rounded-xl border border-slate-300 px-4 py-2"/></label></div>:<div className="my-9 rounded-2xl bg-emerald-50 p-7 text-center"><span className="mx-auto flex h-14 w-14 items-center justify-center rounded-full bg-emerald-500 text-white"><Check/></span><h2 className="mt-4 font-bold">Tu espacio está preparado</h2><p className="mt-1 text-sm text-emerald-800">Podés modificar todo más adelante desde Configuración.</p></div>}<div className="mt-8 flex justify-end"><Button onClick={()=>step<3?setStep(step+1):router.push("/admin/dashboard")}>{step<3?"Continuar":"Ir al inicio"}<ChevronRight size={18}/></Button></div></section></div></main>}
